{
  "name": "hermes",
  "version": "0.1.0",
  "private": true,
  "description": "Serverless multi-domain mail server on AWS",
  "scripts": {
    "build": "bun run build --cwd api",
    "up": "bun run up --cwd infra",
    "preview": "bun run preview --cwd infra",
    "typecheck": "bun run typecheck --cwd api && bun run typecheck --cwd infra",
    "test": "bun test api/tests/api.test.ts --timeout 30000 && bun test web/tests/unit.test.js"
  }
}
